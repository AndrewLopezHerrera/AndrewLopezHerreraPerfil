import Base from "../base/Base";
import PresentationsContent from "./PresentationsContent";
import React from "react";

const Comment: React.FC = () => {

  return (
    <Base frame={<PresentationsContent />} />
  );
};

export default Comment;
import Base from "../base/Base";
import UniversityProjectsContent from "./UniversityProjectsContent";

const UniversityProjects : React.FC = () => {
    return (
        <Base frame={<UniversityProjectsContent />} />
    );
}

export default UniversityProjects;
